<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext HTTP to localhost addresses used during development.

  10.0.2.2  = Android emulator's alias for the host machine's localhost
  localhost  = for Robolectric / unit tests

  Physical device: add your development machine's local IP, e.g.:
      <domain includeSubdomains="false">192.168.1.42</domain>

  All other traffic still requires HTTPS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
